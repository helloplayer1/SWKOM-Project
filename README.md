# PaperlessREST - ASP.NET Core 2.0 Server

No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)

## Run

Linux/OS X:

```
sh build.sh
```

Windows:

```
build.bat
```

## Run in Docker

```
cd src/PaperlessREST
docker build -t PaperlessREST .
docker run -p 5000:5000 PaperlessREST
```
